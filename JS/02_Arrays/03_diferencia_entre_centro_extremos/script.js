function diferenciaCentroExtremos(){
    let valores = [61, 72, 63, 74, 65];
    let suma= (valores[0]+valores[valores.length -1])
    let centro = valores [2]
    let diferencia= centro - suma  
    alert("la diferencia es : \n " 
        + diferencia)}